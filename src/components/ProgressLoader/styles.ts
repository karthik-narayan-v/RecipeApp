import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    height: 6,
    overflow: "hidden",
    marginTop: 48,
  },
  progress: {
    height: "100%",
  },
});

export default styles;
