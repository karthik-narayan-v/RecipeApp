import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  stepRow: {
    flexDirection: 'row',
    gap: 24,
  },
  instructionWrapper: {
    flex: 1,
    flexShrink: 1,
  },
});

export default styles;
